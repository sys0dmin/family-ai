# Family AI Mentor

## План дальнейших улучшений

- Версия: 1.4
- Дата актуализации: 15 августа 2026 года

## Цель

Развивать Family AI как надёжного домашнего голосового наставника для Леры,
сохраняя понятную архитектуру, детскую безопасность, приватность и возможность
независимо заменять инфраструктурные компоненты.

Этот документ дополняет основной `Roadmap.md`. Он фиксирует следующие улучшения
после появления Android-клиента, локальных STT/TTS, нескольких агентов,
родительской админки и инфраструктурного мониторинга.

## Что уже хорошо и не требует переделки

- PostgreSQL остаётся основной базой данных.
- Gateway остаётся единственным местом для бизнес-логики.
- Агенты остаются версионируемой конфигурацией, а не Python-кодом.
- Speech Service остаётся отдельным OpenAI-совместимым сервисом.
- Исходное аудио не сохраняется.
- Облачный speech fallback не включается автоматически.
- Текущие пять голосов Silero сохраняются без подключения лишних TTS-движков.
- Мониторинг ограничивается тремя серверами проекта и не захватывает Proxmox,
  Ceph и остальные домашние ресурсы.

## Порядок реализации

### Этап 0. Завершить разделение safety-входа и safety-выхода

Статус: выполнено и сохранено в Git.

Причина: одинаковая проверка вопроса ребёнка и ответа модели создавала ложные
блокировки. Например, безопасный факт про кровь осьминога заменялся аварийным
fallback.

Задачи:

- проверять входящий вопрос консервативным набором правил;
- проверять ответ модели только на конкретные опасные инструкции и раскрытие
  чувствительных значений;
- разрешать познавательные и предупреждающие упоминания потенциально опасных
  тем;
- журналировать идентификатор сработавшего правила без текста ответа;
- покрыть ложные и настоящие срабатывания автоматическими тестами.

Готово, когда один и тот же безопасный вопрос стабильно получает содержательный
ответ, а явная инструкция взять спички и развести огонь по-прежнему блокируется.

### Этап 1. Наблюдаемость голосового конвейера

Статус: выполнено 23 июля 2026 года.

Задачи:

- [x] измерять отдельно длительность записи, STT, LLM, TTS и полного voice turn;
- [x] показывать текущую очередь Speech Service и активный тип inference;
- [x] считать успешные запросы, ошибки и этап сбоя;
- [x] добавить эти данные во вкладку «Инфраструктура» без хранения аудио и текста;
- [x] показывать уверенность последнего STT для настройки порогов.

Готово, когда из админки видно, где именно Лера ждёт и почему CPU Speech Service
находится под полной нагрузкой.

Архитектурное решение: расширить ADR мониторинга приложения-level метриками, не
добавляя Prometheus или отдельную time-series базу до появления реальной
необходимости.

### Этап 2. Тестовая студия агентов и голосов

Статус: выполнено 23 июля 2026 года.

Задачи:

- [x] добавить в админку отдельный безопасный тестовый режим;
- [x] позволить выбрать агента, ввести контрольную фразу и прослушать его голос;
- [x] показывать сырой ответ модели, результат после safety и идентификатор
  сработавшего правила;
- [x] не добавлять тестовые сообщения в детскую историю;
- [x] добавить короткие предустановленные проверки для всех пяти голосов Silero.

Готово, когда родитель может проверить новую ревизию prompt, safety и голос до
публикации и без создания тестового диалога Леры.

### Этап 3. Настройка STT под детскую речь

Статус: выполнен 24 июля 2026 года, включая первый полевой замер на J3710.

Задачи:

- [x] добавить локальный initial prompt со словами «Лера», «Мурка», «Байтик» и
  именами остальных агентов;
- [x] возвращать техническую оценку уверенности и признак отсутствия речи;
- [x] различать тишину, слишком короткую запись и низкую уверенность;
- [x] реализовать автоматическое сравнение VAD вкл./выкл. и beam size 1/3/5
  непосредственно на J3710 с точностью, отбрасыванием тишины и задержкой;
- [x] подготовить серверный набор из 12 известных фраз и 3 проверок тишины без
  помещения записей Леры в Git, историю сообщений или облако;
- [x] пройти первую полевую калибровку и применить рекомендованные VAD/beam из
  фактического отчёта.
- [x] добавить в защищённую админку изменение beam/VAD, постоянный runtime-конфиг
  и проверяемый перезапуск только `family-ai-speech.service`.

Результат первого замера: `VAD=true`, `beam_size=5`, точность 53%,
отбрасывание тишины 100%, среднее время 7,95 секунды, P95 10,4 секунды.
Перестановка ребёнком слов считается текущей WER-метрикой ошибкой, поэтому
процент отражает строгое совпадение фразы и не является оценкой понятности речи.

Готово, когда имена агентов и типичные детские фразы распознаются устойчивее, а
неудачная запись получает понятный повтор без случайной транскрипции.

### Этап 4. Формальный Safety Policy Engine

Статус: выполнено 24 июля 2026 года.

Задачи:

- [x] заменить разрозненные решения контрактом `ALLOW / TRANSFORM / BLOCK`;
- [x] присвоить каждому правилу стабильный идентификатор и категорию;
- [x] разделить политики входа, выхода, инструментов и permissions агента;
- [x] добавить таблицу сценариев с безопасными фактами, опасными инструкциями и
  пограничными формулировками;
- [x] показывать агрегированные срабатывания в админке без содержания сообщений;
- [x] дать родителю из админки запуск контрольной матрицы, сброс только
  агрегированных счётчиков и переход к разрешениям агентов;
- [x] запретить отключение обязательных правил через prompt или админку.

Готово, когда каждое решение объяснимо правилом, тестируется отдельно и не
зависит от случайного совпадения одиночного слова.

Архитектурное решение: [`ADR 021`](../docs/adr/021-formal-safety-policy-engine.md).
Эксплуатация и каталог правил:
[`docs/safety-policy.md`](../docs/safety-policy.md).

### Этап 5. Разделение provider-интерфейсов

Статус: выполнено и развёрнуто в production 28 июля 2026 года.

Приоритет: архитектурный. Выполнить до подключения нового LLM или speech engine.

Задачи:

- [x] выделить `ChatProvider`;
- [x] выделить `SpeechRecognitionProvider`;
- [x] выделить `SpeechSynthesisProvider`;
- [x] оставить совместимый OpenAI adapter для каждого контракта;
- [x] внедрять провайдеры в сервисы независимо;
- [x] сохранить текущие публичные REST API без изменений.

Готово, когда OpenAI-compatible LLM, локальный STT и локальный TTS можно
переключать независимо без создания искусственного общего provider-объекта.

Реализованы отдельные OpenAI-compatible adapter и dependency factory. Для STT и
TTS можно задать собственные Base URL и API-ключи в админке; пустые значения
сохраняют прежний fallback на общие Speech-настройки.

Архитектурное решение:
[`ADR 025`](../docs/adr/025-separate-ai-provider-contracts.md).

### Этап 6. Модульность Admin UI и Android-клиента

Статус: выполнено 24 июля 2026 года.

Задачи:

- [x] разделить `admin/panel.html` на CSS, ES-модули и небольшие экранные
  компоненты;
- [x] не добавлять UI-фреймворк без отдельной доказанной необходимости;
- [x] разделить Flutter `chat_screen.dart` на состояние диалога, voice controller
  и независимые виджеты;
- [x] оформить состояния `слушаю → понимаю → думаю → отвечаю → ошибка`;
- [x] добавить отмену ожидающего голосового хода с игнорированием позднего
  ответа.

Готово, когда изменение одного экрана или состояния не требует правок в огромном
монолитном файле, а переходы voice UX покрыты widget-тестами.

Архитектурное решение:
[`ADR 022`](../docs/adr/022-modular-admin-and-mobile-chat.md).

### Этап 7. Воспроизводимое развёртывание

Статус: выполнено 25 июля 2026 года и проверено на production.

Задачи:

- [x] создать одну идемпотентную команду развёртывания каждого сервиса;
- [x] собирать пакет только из конкретного Git commit;
- [x] применять миграции отдельно и явно;
- [x] выполнять health-check после перезапуска;
- [x] хранить номер развёрнутой версии и позволять откатить код сервиса;
- [x] не помещать секреты в пакет, stdout или Git.

Готово, когда Gateway, Admin и Speech обновляются одинаковым предсказуемым
процессом без ручного копирования отдельных файлов.

Gateway/Admin и Speech переведены на каталоги релизов в `/srv/family-ai`,
точные окружения по хэшу `uv.lock` и внешнюю конфигурацию в `/etc/family-ai`.
Проверены: детерминированная сборка, отдельный запуск Alembic, автоматический
health-check, автоматический возврат после неуспеха, ручной rollback на legacy и
повторное развёртывание Speech.

Архитектурное решение:
[`ADR 023`](../docs/adr/023-reproducible-shell-releases.md).
Эксплуатационная инструкция:
[`docs/deployment.md`](../docs/deployment.md).

### Этап 8. Родительская долгосрочная память

Статус: выполнено и развёрнуто в production 28 июля 2026 года.

Задачи:

- [x] хранить интересы, предпочтения и учебный прогресс отдельно от 10-дневной
  истории сообщений;
- [x] добавлять только структурированные факты с источником и датой;
- [x] позволить родителю просматривать, исправлять и удалять каждую запись;
- [x] запретить модели самостоятельно превращать предположение в долгосрочный факт;
- [x] не использовать память для давления, удержания внимания или замены родителей.

Готово, когда агент помнит подтверждённые интересы Леры после очистки истории, а
родитель полностью контролирует содержимое памяти.

Реализован изолированный provider-neutral домен `gateway.app.memory` с
repository-портом. Детский и provider API не имеют операции записи; CRUD
доступен только через защищённую вкладку «Память». Изменение повторно
подтверждает факт, удаление физическое, а родительская заметка об источнике не
передаётся модели.

Архитектурное решение:
[`ADR 024`](../docs/adr/024-parent-managed-long-term-memory.md).
Политика и API:
[`docs/long-term-memory.md`](../docs/long-term-memory.md).

## Текущее состояние на 1 августа 2026 года

Выполнены и проверены все этапы 0–8:

1. формальный Safety Policy Engine и раздельная проверка входа/выхода;
2. метрики voice-конвейера, тест-студия и полевая STT-калибровка на J3710;
3. модульные Admin UI и Android-клиент;
4. воспроизводимое развёртывание из точного Git commit с health-check и rollback;
5. родительская долгосрочная память, развёрнутая в production;
6. независимые контракты и настройки для LLM, STT и TTS.

Планы версий 1.2 и 1.3 закрыты, кроме намеренно вынесенной полевой проверки DR
на трёх чистых VM. Мультимодальный голосовой сценарий, Voice 2.0, родительская
обратная связь и голосовые занятия развёрнуты в production.

Дополнение от 28 июля 2026 года: вне этапов 0–8 реализован первый безопасный
Vision-сценарий и космический агент «Алиса Селезнёва». Фото обрабатываются
одноразово независимым provider-контрактом, не сохраняются и доступны только
агентам с инструментом `image_understanding`. См.
[`Roadmap.md`](Roadmap.md) и
[`ADR 027`](../docs/adr/027-ephemeral-image-understanding.md).

Операционная disaster recovery готовность ведётся отдельно:
[`DisasterRecoveryPlan.md`](DisasterRecoveryPlan.md).

## Завершённый цикл: версия 1.3

### Принципы цикла

- Главный пользовательский приоритет — голосовой и визуальный интерфейс, которым
  Лера может пользоваться без чтения.
- Новые возможности добавляются существующим агентам через конфигурацию,
  инструменты и permissions, а не через условные ветки по имени агента.
- Фото и аудио остаются одноразовыми входными данными и не сохраняются.
- Ни один вывод Vision не считается разрешением есть растение, ягоду или гриб,
  использовать лекарство, инструмент либо опасный предмет.
- Долгосрочная память пополняется только после явного подтверждения родителем.
- Механики удержания внимания, серии посещений и бесконечные задания не
  добавляются.

### Этап 9. Закрытие технического baseline

Статус: выполнен 29 июля 2026 года.

Задачи:

- [x] синхронизировать статусы этапов 4–6 в `Roadmap.md` с production;
- [x] устранить коллизию двух ADR с номером `026`, не ломая существующие ссылки;
- [x] проверить ссылки между планами, ADR и эксплуатационной документацией;
- [x] оформить подписанную Android release-сборку вместо debug APK;
- [x] хранить signing secrets только вне Git и документировать восстановление
  ключа подписи;
- [x] добавить воспроизводимую команду сборки APK из точного Git commit;
- [x] описать установку новой release-версии поверх старой без потери адреса
  Gateway и отдельный первый переход с debug-подписи.

Готово, когда документация однозначно отражает production, а Android-клиент
собирается как подписанный релиз из указанного commit без ручного редактирования
исходников и без секретов в репозитории.

Результат:

- Voice, Android и родительская статистика отмечены выполненными в Roadmap;
- clean-room DR сохранил номер ADR 026, Vision перенесён в ADR 027;
- схема подписанных Android-релизов зафиксирована в ADR 028;
- автоматическая проверка подтвердила все локальные ссылки в 60 Markdown-файлах;
- release keystore и properties находятся вне Git, а recovery-kit защищён
  Windows DPAPI CurrentUser;
- тест восстановления signing-kit выполнен в изолированный каталог;
- `Build-AndroidRelease.ps1` успешно собрал APK из commit `002cfb4`, проверил
  package и fingerprint сертификата, выпустил SHA-256/manifest и удалил
  временное дерево исходников;
- release-to-release обновление сохраняет SharedPreferences с адресом Gateway;
  первый переход с debug требует однократной переустановки из-за обязательной
  проверки подписи Android.

Архитектурное решение:
[`ADR 028`](../docs/adr/028-signed-reproducible-android-releases.md).
Runbook:
[`docs/android-release.md`](../docs/android-release.md).

### Этап 10. Проверка Disaster Recovery на чистых VM

Статус: backlog. Runbook и автоматизация готовы, но полевой fire drill отложен до
расширения Proxmox-кластера с трёх до пяти нод: сейчас для трёх чистых VM нет
свободных ресурсов, а останавливать медиатеку и её контейнеры ради проверки не
планируется.

Задачи:

- [ ] подготовить три изолированные тестовые VM с SSH-ключами;
- [ ] выполнить сценарий `TotalLoss`;
- [ ] выполнить сценарий `DatabaseSalvage`;
- [ ] убедиться, что исходный сервер БД в salvage-сценарии не изменяется;
- [ ] проверить историю, агентов, safety-конфигурацию и родительскую память;
- [ ] проверить Gateway, Admin UI, Speech Service и сетевые ограничения;
- [ ] измерить фактические RTO и RPO;
- [ ] исправить найденные отклонения в скриптах и runbook;
- [ ] записать дату успешного fire drill и правила следующей проверки.

Готово, когда оба сценария выполнены на чистых VM только по документации, все
автоматические проверки проходят, а фактическое время восстановления и
ограничения записаны в `DisasterRecoveryPlan.md`.

### Этап 11. Мультимодальный режим «Покажи и спроси»

Статус: выполнен 29 июля 2026 года.

Пользовательский сценарий:

1. Лера визуально выбирает агента.
2. Делает фотографию или выбирает её из галереи.
3. Голосом задаёт вопрос о фотографии.
4. Видит понятное состояние «слушаю → рассматриваю → думаю → отвечаю».
5. Получает голосовой ответ и может воспроизвести его повторно.

Задачи Gateway:

- [x] определить provider-neutral контракт мультимодального хода;
- [x] связать одно фото, одну временную аудиозапись и один диалоговый ход;
- [x] выполнять STT и Vision независимо, затем передавать ограниченные
  наблюдения в Conversation Service;
- [x] не передавать бинарные данные в историю, память или обычный LLM-контекст;
- [x] сохранить совместимость текущих текстового, голосового и Vision API;
- [x] добавить клиентскую отмену хода и игнорирование запоздавших результатов;
- [x] измерять отдельно STT, Vision, LLM, TTS и полное серверное время;
- [x] измерять реальное время до первого звука на телефоне — выполнено в
  этапе 12 вместе с потоковым транспортом;
- [x] возвращать понятные безопасные ошибки для камеры, файла, STT и Vision.

Задачи агентов:

- [x] Алисе разрешить небо, космические объекты и фантастические вопросы;
- [x] Мурке разрешить животных, растения, следы и походные объекты;
- [x] Байтику разрешить компьютерные детали, серверы, кабели и технику;
- [x] Учителю-другу разрешить рисунки, игрушки, книги и бытовые предметы;
- [x] хранить доступность Vision в конфигурации инструментов агента;
- [x] редактировать разрешение из админки с обязательной серверной валидацией;
- [x] не включать понимание фото другим агентам автоматически.

Задачи Android:

- [x] добавить крупный визуальный выбор «Камера» или «Галерея»;
- [x] добавить голосовой вопрос после выбора фотографии;
- [x] не требовать текстового ввода ни на одном шаге;
- [x] показывать локальное превью только до завершения или отмены хода;
- [x] очищать временное превью и байты после завершения;
- [x] сохранять ручной ввод адреса Gateway;
- [x] покрыть адаптивную раскладку, клавиатуру, отмену, replay и новый
  мультимодальный маршрут widget-тестами.

Safety-требования:

- [x] не идентифицировать людей и не выводить чувствительные признаки;
- [x] не угадывать точный домашний адрес или координаты;
- [x] не подтверждать съедобность грибов, ягод, растений и неизвестной еды;
- [x] не разрешать использование лекарств, оружия, ножей, огня и электрических
  устройств по одному изображению;
- [x] честно разделять наблюдение, предположение и неизвестность;
- [x] добавить регрессионную матрицу безопасных и пограничных фотографий без
  использования личных фото Леры в Git.

Готово, когда Лера может без чтения сфотографировать объект, задать вопрос
голосом и услышать ответ подходящего агента, а исходные фото и аудио невозможно
получить после завершения запроса.

Контракт, время жизни данных и границы отмены зафиксированы в
[`ADR 029`](../docs/adr/029-ephemeral-spoken-image-turns.md). Эксплуатационный
runbook: [`docs/multimodal-spoken-image.md`](../docs/multimodal-spoken-image.md).

Обычный one-shot HTTP остаётся совместимым fallback. Физическая отмена
потокового хода и метрика первого звука реализованы в этапе 12.

### Этап 12. Voice 2.0: уменьшение воспринимаемой задержки

Статус: выполнен 1 августа 2026 года.

Задачи:

- [x] зафиксировать baseline one-shot на реальном J3710: первый звук возможен
  только после полного server time 29,5 с плюс передача и запуск плеера;
- [x] отделить полную длительность ответа от воспринимаемой задержки;
- [x] реализовать синтез и воспроизведение ответа по завершённым предложениям;
- [x] не начинать TTS до прохождения обязательной output-safety проверки;
- [x] реализовать потоковый транспорт без привязки Conversation Service к
  конкретному TTS-провайдеру;
- [x] сохранить повторное воспроизведение уже полученных WAV-частей ответа;
- [x] корректно отменять генерацию, оставшийся синтез и воспроизведение;
- [x] не смешивать аудио двух ходов при быстрых нажатиях или смене агента;
- [x] сохранить крупное визуальное действие повторного воспроизведения;
- [x] показать метрики первой части, первого звука, chunks и отмен в админке;
- [x] сравнить выигрыш с дополнительной нагрузкой CPU и сложностью очереди:
  на двух реальных мультимодальных ходах первый звук пришёл через 15,3–15,94 с
  вместо one-shot baseline 29,5 с, то есть воспринимаемая задержка сократилась
  примерно на 46–48%; очередь Speech осталась свободной, ошибок не было.

Готово, когда первый безопасный фрагмент ответа начинает звучать заметно раньше
полного завершения TTS, отмена останавливает весь конвейер, а обычный
непотоковый режим остаётся рабочим fallback.

Контракт, safety-порядок, отмена и выбор NDJSON вместо SSE/WebSocket описаны в
[`ADR 030`](../docs/adr/030-ndjson-voice-streaming.md). Проверка и эксплуатация:
[`docs/voice-streaming.md`](../docs/voice-streaming.md).

### Этап 13. Родительская обратная связь и регрессионное качество

Статус: выполнен 1 августа 2026 года.

Задачи:

- [x] добавить в просмотр истории действие «Отметить проблему»;
- [x] использовать стабильные причины: фактическая ошибка, не понял вопрос,
  слишком сложно, ложная блокировка, нарушен характер, плохой голос или Vision;
- [x] хранить оценку отдельно от сообщения и удалять её вместе с сообщением;
- [x] показывать агрегированные причины на дашборде;
- [x] позволить родителю вручную создать обезличенный regression case;
- [x] перед сохранением тест-кейса показывать и редактировать весь его текст;
- [x] никогда не переносить сообщения Леры в постоянный тестовый набор
  автоматически;
- [x] запускать подтверждённые кейсы в тест-студии до публикации prompt;
- [x] сравнивать итоговый ответ, safety action, rule id и техническую ошибку;
- [x] документировать удаление и экспорт набора проверок.

Готово, когда родитель может объяснить, чем плох конкретный ответ, увидеть
повторяющиеся категории проблем и осознанно превратить обезличенный пример в
регрессионную проверку без скрытого бессрочного хранения детской истории.

Жизненный цикл зафиксирован в
[`ADR 031`](../docs/adr/031-parent-feedback-regression-lifecycle.md), а
родительский workflow, retention, удаление и экспорт — в
[`docs/parent-quality.md`](../docs/parent-quality.md).

### Этап 14. Голосовые приключения и занятия

Статус: выполнен 1 августа 2026 года.

Задачи:

- [x] определить короткий конфигурационный формат сценария, не превращая агента
  в отдельный Python-класс;
- [x] добавить сценарии «Космическая экспедиция», «Поход с Муркой»,
  «Собираем компьютер», «Отгадай животное» и совместную сказку;
- [x] хранить состояние незавершённого занятия отдельно от system prompt;
- [x] позволить остановить занятие или перейти к обычному разговору в любой
  момент;
- [x] сделать управление голосом и крупными пиктограммами без обязательного
  чтения;
- [x] ограничить занятие естественным коротким финалом;
- [x] не добавлять серии дней, награды за удержание и бесконечную ленту;
- [x] предлагать учебный результат родителю на подтверждение, не записывая его
  автоматически в долгосрочную память;
- [x] добавить в тест-студию предпросмотр сценария и сброс его состояния.

Готово, когда каждый сценарий имеет начало, несколько понятных шагов и
естественный финал, переживает перезапуск клиента и не мешает обычному диалогу.

Модель состояния, retention и границы сервисов зафиксированы в
[`ADR 032`](../docs/adr/032-configured-activity-sessions.md), эксплуатация и API —
в [`docs/activities.md`](../docs/activities.md).

## Следующий эксплуатационный цикл: версия 1.4

Цель цикла — не расширять Family AI новыми крупными функциями, а подтвердить
надёжность уже реализованного продукта в повседневном использовании.

### Этап 15. Стабилизация на реальном использовании

Статус: backlog до замены аккумулятора детского телефона. Небезопасный вздувшийся
аккумулятор не используется даже для тестирования.

- [ ] провести не менее одной недели обычного использования без искусственного
  тестового сценария;
- [ ] отмечать проблемные ответы только явным родительским действием в админке;
- [ ] сгруппировать наблюдения по STT, LLM, TTS, Vision, safety и интерфейсу;
- [ ] исправлять повторяющиеся причины, а не единичные вкусовые отклонения;
- [ ] прогнать подтверждённый regression suite после исправлений;
- [ ] записать итоги цикла без переноса личных сообщений и записей Леры в Git.

Готово, когда неделя эксплуатации не выявляет критических сбоев, а повторяемые
проблемы либо исправлены, либо явно приняты и документированы.

### Этап 16. Сквозной smoke-test релиза

Статус: выполнен и развёрнут в production 2 августа 2026 года.

- [x] после развёртывания проверять не только процессы и HTTP health-check;
- [x] проверить цепочку Admin UI → Gateway → PostgreSQL;
- [x] выполнить безопасные синтетические проверки LLM, STT и TTS без данных Леры;
- [x] проверить доступность Vision без сохранения тестового изображения;
- [x] при сбое не помечать release успешным и выводить конкретный этап ошибки;
- [x] сохранить возможность отдельно разворачивать и откатывать каждый сервис;
- [x] выполнить полный smoke-test на production и записать фактический результат.

Готово, когда одна воспроизводимая команда подтверждает работоспособность
пользовательского контура, не создавая детскую историю и долгосрочную память.

Решение и компромиссы зафиксированы в
[`ADR 034`](../docs/adr/034-functional-release-smoke-test.md), команда и порядок
отката — в [`docs/deployment.md`](../docs/deployment.md).

Первый production-прогон на commit `d9c5e14` успешно проверил Admin UI,
Gateway/PostgreSQL, LLM, TTS, STT и Vision. Самым долгим этапом остался локальный
STT — 9,2 секунды; весь релиз принят без отката.

### Этап 17. Визуальные регрессии Admin UI и Android

Статус: выполнен 2 августа 2026 года.

- [x] зафиксировать поддерживаемые размеры desktop и mobile для Admin UI;
- [x] покрыть критические экраны эталонными скриншотами без production-данных;
- [x] проверить Android в портретной и альбомной ориентации с закрытой и
  открытой клавиатурой;
- [x] проверять переполнение, перекрытие кнопок и доступность основных действий;
- [x] выполнять визуальную проверку перед публикацией UI-релиза.

Готово, когда известные случаи `overflow` и наложения элементов обнаруживаются
до развёртывания, а изменение эталона требует осознанного подтверждения.

Поддерживаемые viewport, команды и процедура обновления описаны в
[`docs/visual-regression.md`](../docs/visual-regression.md), архитектурное
решение — в [`ADR 035`](../docs/adr/035-visual-regression-baselines.md).

### Этап 18. Локальные эксплуатационные предупреждения

Статус: выполнен и развёрнут в production 2 августа 2026 года; миграция
`018_add_operational_alerts` применена успешно.

- [x] определить пороги свободного диска, очереди Speech и серии ошибок;
- [x] показывать активные предупреждения в существующей вкладке инфраструктуры;
- [x] не подключать внешний мониторинг домашнего кластера;
- [x] не отправлять наружу тексты, аудио, фото или идентификаторы диалогов;
- [x] предусмотреть подтверждение и историю только технических событий.

Готово, когда родитель видит деградацию сервисов до жалобы пользователя, но
мониторинг остаётся внутри контура Family AI.

Реализованы настраиваемые пороги `15/8%` свободного диска, `2/4` запроса в
очереди Speech и `3/5` voice-ошибок подряд. Активный эпизод можно подтвердить,
но он закрывается только после восстановления; повторная деградация создаёт новый
эпизод. Закрытая техническая история хранится 30 дней. Решение описано в
[`docs/operational-alerts.md`](../docs/operational-alerts.md) и
[`ADR 036`](../docs/adr/036-local-operational-alerts.md).

### Этап 19. Синтетическая эксплуатационная проверка

Статус: выполнен и развёрнут в production 15 августа 2026 года на commit
`a071d25`. Admin-самопроверка прошла все семь изолированных сценариев.

- [x] добавить фиксированный обезличенный набор фраз без записей Леры;
- [x] измерять LLM, TTS, STT и полный цикл при параллельности `1/2/4`;
- [x] считать p50/p95, ошибки, similarity, очередь Speech и ресурсы серверов;
- [x] проверять неизменность числа сообщений до и после stateless benchmark;
- [x] не сохранять аудио, транскрипцию, LLM-ответы и идентификаторы диалогов;
- [x] добавить изолированную Admin-самопроверку warning → critical →
  acknowledgement → recovery → repeat;
- [x] не заполнять production-диск и не останавливать реальные сервисы;
- [x] выполнить полный production benchmark и записать обезличенную сводку.

Готово, когда bounded-прогон воспроизводимо показывает предел текущего J3710,
не изменяет детскую историю, а все lifecycle-сценарии предупреждений проходят в
одноразовой БД.

Команда, формат отчёта и интерпретация описаны в
[`docs/voice-soak.md`](../docs/voice-soak.md), самопроверка — в
[`docs/operational-alerts.md`](../docs/operational-alerts.md), решение — в
[`ADR 037`](../docs/adr/037-synthetic-operational-verification.md).

Baseline J3710: `14/14` успешных циклов, ошибок `0`, история не изменилась.
Total p95 вырос с `14,5 с` при одном клиенте до `25,0 с` при двух и `49,0 с`
при четырёх; максимальная очередь составила `0/1/3`. Speech CPU достиг p95
`94,4%`, но память осталась стабильной на уровне не выше `15,4%`. Порог
предупреждения `queue >= 2` подтверждён фактическим замером.

### Этап 20. Безопасный lifecycle runtime-конфигурации

Статус: завершён 15 августа 2026 года. Полный regression suite пройден локально,
release развёрнут на Gateway, а безопасный production-цикл preview → apply →
readiness → rollback подтверждён на реальной конфигурации.

- [x] ограничить ревизию allow-list параметров, доступных во вкладке «Настройки»;
- [x] добавить предварительную валидацию и redacted diff без записи;
- [x] писать environment-файл атомарно;
- [x] хранить bounded-историю локально на Gateway с закрытыми правами;
- [x] после применения перезапускать Gateway и ждать loopback readiness;
- [x] при ошибке автоматически вернуть предыдущий файл и проверить восстановление;
- [x] не откатывать БД, пароль Admin, monitoring и deployment-переменные;
- [x] добавить компенсирующий возврат beam/VAD при ошибке Speech restart;
- [x] завершить полный regression suite Gateway, Speech, Admin и Android;
- [x] выполнить production-проверку apply/rollback после публикации release.

Готово, когда изменение из Admin либо становится подтверждённой активной
ревизией, либо автоматически возвращается к прежнему состоянию без ручного SSH.
Контракт описан в
[`docs/runtime-configuration.md`](../docs/runtime-configuration.md), решение — в
[`ADR 038`](../docs/adr/038-safe-runtime-configuration-lifecycle.md). Безопасный
root-mediated restart без `sudo` из Admin описан в
[`ADR 039`](../docs/adr/039-root-mediated-gateway-restart.md).

### Этап 21. Сквозная техническая трассировка и диагностический пакет

Статус: запланирован после этапа 20.

- [ ] назначать обезличенный `request_id` в Android/Gateway;
- [ ] передавать его через STT, LLM, TTS и Vision adapters;
- [ ] записывать структурированные технические события без контента;
- [ ] показывать в Admin временную шкалу конкретного неуспешного turn;
- [ ] экспортировать redacted diagnostic bundle без сообщений, аудио, фото и ключей.

### Этап 22. Управление перегрузкой Voice

Статус: запланирован после этапа 21.

- [ ] определить максимальную допустимую очередь на основании Stage 19 benchmark;
- [ ] отменять устаревшие задания при выходе из voice screen;
- [ ] защищаться от повторной отправки одного запроса;
- [ ] ввести отдельные time budgets STT, LLM и TTS;
- [ ] возвращать понятное детское сообщение вместо бесконтрольного ожидания.

### Этап 23. Паспорт запущенных версий

Статус: запланирован.

- [ ] показывать commit Gateway и Speech, Alembic head и uptime;
- [ ] показывать версию Android и безопасный fingerprint конфигурации;
- [ ] явно отмечать расхождение production с ожидаемым release commit.

### Этап 24. Единый локальный release gate

Статус: запланирован.

- [ ] объединить Ruff, pytest Gateway/Speech, Flutter analyze/test, visual suite,
  Markdown links и проверку release archive одной локальной командой;
- [ ] проверять случайное добавление секретов и крупных артефактов;
- [ ] не зависеть от внешнего CI и не отправлять данные домашнего контура наружу.

### Этап 25. Родительская карта обучения

Статус: backlog до реального эксплуатационного цикла Леры.

- [ ] позволить родителю выбрать несколько учебных направлений и текущие цели;
- [ ] предлагать короткие голосовые занятия без чтения и механик зависимости;
- [ ] сохранять результат в долгосрочную память только после явного подтверждения;
- [ ] не вводить streak, рейтинг, бесконечные задания или автоматическое профилирование.

## Актуальный порядок работ

1. Завершить этап 20 — безопасное применение и rollback конфигурации.
2. Выполнить этап 21 — request tracing и redacted diagnostic bundle.
3. Выполнить этап 22 — bounded queue, cancellation и time budgets Voice.
4. Выполнить этап 23 — паспорт реально запущенных версий.
5. Выполнить этап 24 — единый локальный release gate.
6. Вернуться к этапу 10 после расширения Proxmox-кластера до пяти нод.
7. Вернуться к этапам 15 и 25 после замены аккумулятора телефона и недели
   обычного использования Лерой.

Новые агенты, движки и инфраструктурные компоненты не являются приоритетом этого
цикла. Замена оборудования Speech Service рассматривается отдельно после замера
реальной очереди и времени ожидания.

## Не включено в эксплуатационный цикл 1.4

- wake word и постоянное прослушивание микрофона;
- автоматическая отправка детского аудио в облачный fallback;
- новые TTS-движки только ради увеличения числа голосов;
- локальная LLM на текущем J3710;
- мониторинг Proxmox, Ceph и посторонних контейнеров;
- резервное копирование в рамках текущего домашнего MVP;
- публичный доступ из интернета.

## Постоянные границы продукта

Следующие пункты не являются будущим backlog и не могут быть включены настройкой
агента или админки:

- автоматическое бессрочное хранение фото, аудио или сообщений;
- распознавание личности по фотографии;
- медицинская диагностика по изображению;
- определение съедобности или безопасности предмета только по фотографии;
- автоматическое пополнение долгосрочной памяти моделью;
- механики зависимости: серии дней, таймеры возврата и бесконечные задания.

Анализ изображения, описание наблюдаемых признаков и предложение учебного
результата разрешены. Запрещено выдавать предположение за достоверный факт или
совершать постоянное действие без явного подтверждения родителя. Контракт и его
программные гарантии описаны в
[`docs/child-safety-boundaries.md`](../docs/child-safety-boundaries.md) и
[`ADR 033`](../docs/adr/033-non-negotiable-child-data-boundaries.md).
